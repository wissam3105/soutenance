{% extends 'base.html.twig' %}

{% block title %}Mon Recapitukatif!{% endblock %}

{% block body %}
<style>
  .form-check {
        background-color: #f4f8ff;
        padding: 20px 65px;
        margin-top: 7px;
    }
    .col-form-label {
        font-weight:bold;
    }
    .order-summary{
       background-color: #f4f8ff;
       padding-left: 20px;  
        margin-top: 22px;
    }
</style>
<br> <br>
<h4>Mon Recapitukatif!</h4>
<p>Verifiez vos information!</p>

<hr>
<div class= "row">
   <div class="col-md-6">
   
   
   
   </div>
   <div class="col-md-6">
   Mon adresse de livraison </br>
   mon transporteur

   <div class="order-summary">
  
   <b>Recap de ma commande</b></br>
   <b>vous trouvez le recap de vos produit</b>
    <br>
   
       <table>
    <thead>
        <tr>
            <th>Image</th>
            <th>Titre</th>
            <th>Prix unitaire</th>
            <th>Quantité</th>
            <th>Total</th>
        </tr>
    </thead>
    <tbody>
        {% for item in cart %}
        
            <tr>
                <td><img src="/assets/produits/{{ item.article.image }}" height="75px"></td>
                <td>{{ item.article.title }}</td>
                <td>{{ item.article.prix }}</td>
                </br><td>{{ item.quantite }}</td>
                <td>{{ item.article.prix * item.quantite }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>

<br>
<hr>
   </div>
   <a href="{{path('paiement')}}"  class="btn btn-success btn-block mt-3">Payer</a>
</div>
</div>

{% endblock %}